from datetime import date
import os
import csv
timestamp_seconds = date.today()
print(f"date of : {timestamp_seconds}")

current_derectory = os.getcwd()
print(current_derectory)
print("please input name of csv file for tests")
file_name = input()
headers = "date,time,errors\n"
absolute_path = os.path.join(f"{current_derectory}\{file_name}.csv")
print(absolute_path)

with open(f"{absolute_path}", "w") as file_path:
            file_path.write(headers)


